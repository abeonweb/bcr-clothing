import Directory from "../../components/directory/directory.component";
import { Fragment } from "react";

const Home = () => {
  return (
    <Fragment>
      <Directory />
    </Fragment>
  );
};

export default Home;
